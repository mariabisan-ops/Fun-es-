#include <stdio.h>

int inverso (int);

int main(){
  int n;
  printf (“digite um valor: “);
  scanf (“%d”, &n);
  printf (“inverso: %d”, inverso (n));
}

int inverso (int n){
  int inv = 0;
  while (n>0){
    inv = inv*10 + n%10;
    n = n/10;
  }
  return inv;
}
