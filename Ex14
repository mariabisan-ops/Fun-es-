#include <stdio.h>

int ehprimo(int x);

int primos(int [], int);

int main(){
    int n;
    int v[100];
    int q;
    printf("Digite um numero: ");
    scanf("%d", &n);

    q = primos (v, n);

    printf("Fatores primos: ");

    for(int i = 0; i < q; i++){
       printf("%d ", v[i]);
    }
}

int ehprimo(int x){
    if(x =1){
        return 0;
    }

    for(int i = 2; i < x; i++){
       if(x % i == 0){
          return 0;
        }
    }
    return 1;
}

int primos (int v[], int y){

    int cont = 0;

    for(int i = 2; i <= y; i++){
        if(n % i == 0 && ehprimo(i)){
            vetor[cont] = i;
            cont++;
        }
    }
    return cont;
}
