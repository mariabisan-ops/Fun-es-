#include <stdio.h>

int soma (int);

int main (){
  int z;
  printf ("digite um valor: ");
  scanf ("%d", &z);

  printf ("soma: %d", soma(z));
}

int soma (int n){
  int soma =0;
  for (int i=1; i<=n; i++){
    soma = soma + i;
  }
  return soma;
}
