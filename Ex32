#include <stdio.h>

int celsius (int [], int[]);

int main(){
    int f[101];
    int c[101];
    for (int i = 50; i<=150; i++){
        f[i-50]= i;
    }
    for (int i=50; i<=150; i++){
        printf ("farenheit: %d", f[i]);
    }
    printf ("\ncelsius: %d ", celsius (c, f));
}

int celsius (int c[], int f[]){
    for (int i=0; i<=150; i++){
        c[i-50] = (5*(f[i-50]-32))/9;
    }
}
